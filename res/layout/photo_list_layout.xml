<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:hkui="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >
    
    <GridView
        android:id="@+id/image_grid_list"
        android:layout_marginTop="75px"
        android:layout_marginLeft="20px"
        android:layout_marginRight="20px"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:listSelector="@drawable/image_item_bg"
        android:scrollbarThumbVertical="@drawable/scrollbar_thumb"
        android:verticalSpacing="15px"
        android:numColumns="3" />
    
    <TextView
        android:id="@+id/image_list_empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/media_no_file"
        android:textColor="@color/hk_custom_text"
        android:textSize="26sp" />
    
    <include layout="@layout/media_loading_fragment"
        android:id="@+id/loading_layout"
        android:layout_centerInParent="true" />

</RelativeLayout>